# Confidence Interval 置信区间
#mg-gy6193 #concept #inference

区间估计的一般形式：
- estimate ± critical value × standard error。

例：总体比例 p 的单样本 z 区间：
- p-hat ± z* × sqrt(p-hat(1−p-hat)/n)。

置信度（如 95%）描述的是方法在长期重复下“覆盖真实参数”的频率。
